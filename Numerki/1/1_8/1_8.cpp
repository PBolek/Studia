#include <bits/stdc++.h>

using namespace std;

float function2(float x){
    float a, b, c;
    cout << "podaj a, b, c, dla funkcji postaci ax^2 + bx + c = 0";
    cin >> a >> b >> c;

}

int main()
{

	return 0;
}
